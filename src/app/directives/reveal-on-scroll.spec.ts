import { RevealOnScrollDirective } from './reveal-on-scroll';

describe('RevealOnScroll', () => {
  it('should create an instance', () => {
    const directive = new RevealOnScrollDirective(null as any, null as any);
    expect(directive).toBeTruthy();
  });
});
